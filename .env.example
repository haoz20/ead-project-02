# Backend Configuration
PORT=4000

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# S3 Configuration
S3_BUCKET_NAME=your-s3-bucket-name

# Database Configuration (AWS RDS PostgreSQL)
DB_HOST=your-rds-endpoint.rds.amazonaws.com
DB_PORT=5432
DB_NAME=videodb
DB_USER=your-db-username
DB_PASSWORD=your-secure-password
DB_SSL=true

# Frontend API URL (for production, use your EC2 public IP or domain)
# Example: http://your-ec2-ip:4000 or https://api.yourdomain.com
VITE_API_BASE=http://localhost:4000

# CORS Configuration (comma-separated list of allowed origins, or * for all)
# Example: http://your-ec2-ip:80,https://yourdomain.com
CORS_ORIGIN=*
